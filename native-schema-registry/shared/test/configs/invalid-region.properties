# Expected behavior: Should throw UnknownHostException
# This config uses an invalid region (us-east-99) that doesn't exist,
# causing DNS resolution to fail when trying to connect to the endpoint
region=us-east-99
registry.name=default-registry
dataFormat=AVRO
schemaAutoRegistrationEnabled=true
