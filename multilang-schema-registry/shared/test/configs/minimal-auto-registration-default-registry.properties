# Expected behavior: Should successfully auto-register schema in the default registry
# This config enables auto-registration without specifying a registry name,
# so GSR will use the default registry for schema registration
region=us-east-1
dataFormat=AVRO
schemaAutoRegistrationEnabled=true
