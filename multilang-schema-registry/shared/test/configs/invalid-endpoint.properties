# Expected behavior: Should throw UnknownHostException
# This config uses an invalid endpoint URL that doesn't exist,
region=us-east-1
registry.name=default-registry
endpoint=https://invalid-endpoint.amazonaws.com
dataFormat=AVRO
schemaAutoRegistrationEnabled=true
