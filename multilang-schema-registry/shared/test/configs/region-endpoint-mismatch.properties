# Expected behavior: Should throw AccessDenied/credential scoping exception
# This config has mismatched region (eu-west-2) and endpoint (eu-west-1),
# causing IAM credentials to fail validation due to region mismatch
region=eu-west-2
registry.name=default-registry
endpoint=https://glue.eu-west-1.amazonaws.com
dataFormat=AVRO
schemaAutoRegistrationEnabled=true
