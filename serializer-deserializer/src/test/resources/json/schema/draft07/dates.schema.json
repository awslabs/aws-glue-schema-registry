{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "orderDate": {
            "type": "string",
            "format": "date"
        },
        "purchaseDate": {
            "type": "string",
            "format": "date-time"
        },
        "deliveryDate": {
            "type": "string",
            "format": "date-time"
        },
        "expectedDeliveryDate": {
            "type": "string",
            "format": "date-time"
        }
    },
    "required": [ "orderDate", "purchaseDate", "deliveryDate", "expectedDeliveryDate" ]
}